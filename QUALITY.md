# 质量门禁（Quality Gate）

> 适用：本「智能花卉 DIY 智能体」项目。项目以 vibe coding / 低代码方式推进，但要线下落地，
> 故以「自动门禁 + 规范文档 + 可回退」三件套保证工程质量与健壮性。

## 一、自动门禁（pre-commit 钩子）

- 每次 `git commit` 前自动运行 `npm test`（全套回归：agent 164 / plugins 8 / http 20 / ratelimit 5 / insights 12，共 209 项）。
- **测试不过，提交被阻止**。修复后再提交。
- 启用方式（仓库级，一次性）：
  ```bash
  git config core.hooksPath .githooks
  ```
- 钩子文件：`.githooks/pre-commit`（已纳入版本控制；clone 后执行上面一行即启用）。
- 紧急情况跳过（不推荐，须事后补测并说明原因）：`git commit --no-verify`。

## 二、零依赖底线

- 运行时只用 Node.js 内置能力与零依赖自写模块；**禁止 `npm install` 引入任何第三方包**。
- 例外：仅当某能力 Node 标准库 / 内置（如 `node:sqlite`、`node:test`）无法实现且与用户确认后才可考虑，且须记入《开发日志.md》。

## 三、改动即提交 + 可回退

- 每完成一个验证过的小改动，立即 `git commit` 并打标签 `mN-简述`（如 `m12-data-expand`）。
- 回退依据 =《开发日志.md》+ git 标签：告诉智能体「回退到 Mx」即执行 `git checkout mx`。
- 运行数据（`data/agent.db`）与密钥（`.env`）不入库，回退代码不影响数据资产。

## 四、知识外置 + 专家复核

- 所有规则 / 方案参数 / 花材门店等主数据放在 `data/*.json`，由领域专家审阅，不硬编码在逻辑里。
- 种子数据带 `source` 字段（行业资料 / 花艺师手册 / 平台规则），便于溯源与质检。
- 非花卉专业人员新增 / 修改规则时，先标 `source` 与（规划中）`confidence`，交由专家复核后再生效。

## 五、测试隔离

- 回归测试使用隔离临时库（`FLORA_DATA_DIR` 环境变量），不污染主库、不依赖历史 feedback，保证「固定方案」断言可复现。
- 新增逻辑必须补测试；重构必须跑全套 `npm test` 确认 209 项全绿方可提交。

## 六、安全

- `.env`（含 LLM / 文生图 key）已 gitignore，**绝不入库**。
- 外部 fetch 必须带超时（LLM 20s / 图 30s）并降级，避免无限挂起；错误信息不泄露 key。
- 路径 / 价格 / 权限等已在 M2 修复，新增接口沿用既有校验。

## 七、文档同步

- 接口变更 → 同步《API契约文档.md》；
- 数据结构变更 → 同步《数据模型与采集设计文档.md》；
- 里程碑 → 同步《开发日志.md》。

## 八、提交前自检清单

- [ ] `npm test` 全绿（209 项）
- [ ] 未引入 npm 包
- [ ] `.env` / `data/agent.db*` 未进暂存
- [ ] 新增 / 修改的规则带 `source`
- [ ] 相关文档已同步
- [ ] 已 `git commit` + 打 `mN` 标签
