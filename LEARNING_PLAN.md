# Vibe Coding 入门学习计划（智能花卉 DIY 项目）

> 定位：无编程基础 → 能通过 AI 工具（vibe coding）独立开发本项目及后续 Agent Service
> 周期：6 周（每周约 8-10 小时）
> 核心原则：学"读"不学"写"；小步迭代；每步验证；生产代码需他人评审

## 一、认知校准（开始前必读）

- 目标不是"会写代码"，而是"会读代码 + 会验证 + 会精确描述需求"
- AI 负责实现，你负责：需求定义 / 验证 / 排错 / 部署
- vibe coding 的三个常态：AI 会出错、AI 会编造、AI 会改坏 → 因此强制验证 + git 回滚

## 二、6 周计划

### 第 1 周：通读项目（逐文件讲解：对照 README 与 lib/、data/ 各源文件，按文件提问与过关）
- 任务：让 AI 逐文件讲解本项目的每个文件，按清单提问与过关
- 练习：git 三命令 + GitHub 上传/回滚演练（详见《GITHUB_GUIDE.md》）
- 产物：能向同事复述"一条用户消息从进来到返回走了哪条链路"
- 过关标准：能画出 用户→server.js→decomposer→planner→imageGen→响应 的数据流图

### 第 2 周：JavaScript / Node 最小可读集
- 概念：变量、对象{...}、数组[...]、函数、if/else、require/module.exports、async/await
- 练习：把 lib/pricer.js 和 lib/util.js 各读一遍，向 AI 复述其逻辑，AI 判定是否正确
- 实践任务：往 data/flowers.json 里加一朵新花，用 curl 调用验证方案包含它
- 过关标准：能解释 lib/pricer.js 里 ensureBudget 的 4 级降级顺序

### 第 3 周：数据链路与 API
- 概念：HTTP 请求/响应、GET/POST、URL、JSON、API key 安全、进程与端口
- 练习：用浏览器开发者工具(NETWORK 面板)观察一次 /api/v1/chat 请求的完整往返
- 实践任务：读懂 .env 配置（IMAGE_API_KEY/LLM_API_KEY），学会"为什么 key 不写代码里"
- 过关标准：能解释通义万象"异步任务+轮询"为何需要两步接口

### 第 4 周：vibe coding 工作法实战
- 首个真功能：实现"多轮会话记忆"（lib/agent/sessionStore.js + 追问修改）
- 流程演练：描述需求→AI 实现→要求 AI 解释改动→写验证脚本→git 提交→代码评审
- 过关标准：完成从"方案生成"到"用户说'百合换洋桔梗'→方案局部更新"的闭环

### 第 5 周：微信小程序结构
- 概念：一个页面 = wxml(结构)+wxss(样式)+js(逻辑)+json(配置)；app.json 全局配置
- 练习：通读 miniprogram/ 全部 4 个页面，向 AI 复述 chat 页如何调用后端
- 实践任务：把方案卡片的"花语/养护"区块补全到小程序 plan 页
- 过关标准：能解释"小程序不能直接连通义万象、必须走后端转发"的原因

### 第 6 周：收尾与展望
- 学习 git 完整常用集：status/diff/log/add/commit/checkout 回滚/分支概念
- 写一份《项目架构图 + 个人可维护清单》（让 AI 辅助）
- 明确与同事的评审机制：每周一次代码 review（可由 AI 初评 + 同事终评）
- 进入 Agent Service 开发（复用本计划方法论）

## 三、vibe coding 工作法速查

### 需求描述模板（写给 AI）
```
背景：<项目是什么>
任务：<要做什么，输入是什么，输出是什么>
限制：<不许用什么库/不许改哪个文件>
验证：<我怎么判断你做完没，请给出验证方法>
```

### 每次 AI 改完必问的 4 个问题
1. 你改了什么文件？为什么这么改？
2. 哪里可能出错？验证方法是什么？
3. 给我一个验证命令/测试脚本
4. 有没有引入我不知道的新依赖或新概念？

### 必会 git 命令（第 1 周就学，详见 GITHUB_GUIDE.md）
- git init / git add . / git commit -m "说明" （存档）
- git checkout -- <文件> （撤销未提交改动 = 后悔药）
- git log --oneline （看历史）

### 红线清单
- [ ] key 绝不写进代码或提交 git（.env 已被 .gitignore 排除）
- [ ] 每次改动前先 commit 存档
- [ ] 生产环境改动必须有懂技术的人评审
- [ ] AI 说"完成了"≠完成，必须自己跑一遍验证
- [ ] 不熟悉的库先让 AI 解释再允许引入（本项目应坚持零依赖）

## 四、风险与对策

| 风险 | 对策 |
| --- | --- |
| 代码质量无人把关 | 每周评审（AI 初评+同事终评） |
| 知识断层导致不敢改 | 维护"术语表.md"（让 AI 每次讲新概念时追加） |
| AI 修改破坏功能 | git 回滚 + 每次改前存档 |
| 学习材料过载 | 严格按每周任务走，不看多余教程 |
