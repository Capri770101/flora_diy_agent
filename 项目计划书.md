# 智能花卉 DIY 智能体 · 项目计划书

> 文档类型：项目开发计划（Software Project Management Plan, SPMP）
> 标准依据：GB/T 8567《计算机软件文档编制规范》"项目开发计划" + IEEE 1058 + 阶段门禁（Stage-Gate）方法论
> 版本：v1.0　|　编制日期：2026-08-07　|　编制：智能体（AI 协理）　|　审批：Capri（产品 / 前端 / 业主）

| 项 | 内容 |
|---|---|
| 项目名称 | 智能花卉 DIY 智能体（flora_diy_agent） |
| 项目代号 | FloraAgent |
| 仓库地址 | https://github.com/Capri770101/flora_diy_agent |
| 当前基线 | `c13b137`（main，含 M15–M18 里程碑 + 用户工作区快照） |
| 开发模式 | 单人业主 + AI 协理的"人机协作"增量开发 |
| 技术栈 | Node.js 22（零依赖，内置 node:sqlite）+ 微信小程序原生 |
| 文档状态 | 已发布 v1.0，作为后续开发的标准流程基线 |

---

## 0. 文档说明

本计划书定义项目的**目标、范围、组织、标准开发流程、技术方案、里程碑、质量/配置/风险管理、交付与验收**，是全团队（业主 Capri、AI 协理、领域专家）共同遵循的流程基线。任何偏离本计划的重大变更，须走"变更控制"（见 §3.5）。

> **为何需要本标准流程**：本项目由业主并行开发小程序 UI、AI 协理并行开发后端智能体，存在"双轨并行 + 同仓协作"特点。若无统一流程基线，易出现：提交相互覆盖、无法精确回退、规则盲区"静默半错"、密钥误提交等风险。本计划书即用于消除上述不确定性。

---

## 1. 项目概述

### 1.1 项目背景
花卉定制场景存在"用户说不清需求、花艺师沟通成本高、方案与价格不透明"的痛点。项目目标是构建一个**领域智能体**：用户用自然语言描述（场合 / 对象 / 预算 / 偏好），智能体基于知识库与规则引擎生成可落地的花卉方案、报价、效果图与门店推荐，并通过反馈闭环持续学习。

### 1.2 项目目标
- **功能性目标**：自然语言理解 → 方案生成（花材/数量/价格现算）→ 门店匹配 → 下单 → 反馈回流。
- **架构目标**：智能体为核心、数据主权在服务端；小程序仅为可替换的客户端壳。
- **质量目标**：全套回归测试常绿；知识规则经领域专家质检；任意里程碑可独立回退。
- **约束目标**：零 npm 依赖（仅用 Node 内置能力），保障可移植性与可审计性。

### 1.3 项目范围

**范围内（In Scope）**
- 智能体后端：`server.js` + `lib/agent/*` + CLI（`scripts/agent-cli.js`）+ 数据层。
- 14 域数据底座（种子 + 运行时积累）。
- API 契约（chat/plan/shops/orders/pay/health/preview/feedback 等）。
- 学习闭环（/feedback 回写、/chat 回传信号）。
- 知识质检流水线（source/confidence + 专家复核）。
- 微信小程序客户端（业主自研 UI，AI 出规格 + 审核）。

**范围外（Out of Scope，明确排除）**
- 智能体不直接经营门店 / 物流（仅数据建模与推荐）。
- 不做独立 Web 前端（已于 2026-08-05 移除，CLI 为调试入口）。
- 不替代业主的小程序 UI 开发职责。
- 不引入任何第三方 npm 包（零依赖底线）。

### 1.4 术语
- **智能体（Agent）**：平台核心，含规则引擎、规划器、LLM 适配、数据层、学习闭环。
- **14 域数据底座**：花卉/门店/地区/价格/商家能力/知识/合规/物流/潮流/库存/UGC/营销/竞品/元学习。
- **里程碑标签（mN）**：git tag，标识一个验证过的智能体功能里程碑，用于精确回退。
- **质量门禁**：提交前必须通过的自动化 + 人工检查集合。

---

## 2. 项目组织与角色职责

### 2.1 干系人
| 角色 | 担当 | 职责 |
|---|---|---|
| 产品 / 业主 / 前端 | Capri | 需求定义、小程序 UI、并行开发、最终验收 |
| AI 协理（智能体） | 本 AI | 后端架构、功能实现、测试、文档、审核用户改动 |
| 领域专家（外部） | 花艺师 / 运营 | 知识质检复核（`reviews.json`） |
| 运维（兼） | Capri + AI | 部署、备份、监控（上线阶段） |

### 2.2 RACI 矩阵
| 活动 | Capri | AI 协理 | 领域专家 |
|---|---|---|---|
| 需求与范围 | A/R | C | I |
| 后端实现 | I | R/A | — |
| 小程序 UI | R/A | C（规格+审核） | — |
| 知识质检 | A | C | R（复核） |
| 测试与质量门禁 | A | R | — |
| 提交 / 标签 / 回退 | A | R | — |
| 上线部署 | A/R | C | — |

R=负责执行，A=最终问责，C=被咨询，I=被告知。

---

## 3. 标准开发流程（核心）

本项目采用 **阶段化里程碑门禁（Stage-Gate）** 混合 **敏捷增量** 的轻量标准流程，适配"单业主 + AI"的小团队现实。

### 3.1 流程总览
```
P0 立项 ─G0─▶ P1 需求/范围 ─G1─▶ P2 设计 ─G2─▶ P3 实现+回归 ─G3─▶ P4 集成测试 ─G4─▶ P5 验收上线 ─G5─▶ P6 运营改进 ─G6─┐
   ▲__________________________________________________________________________________________________________│
   └──────────────────────────── 复盘 / 变更控制回灌 ───────────────────────────────────────────────────────┘
```
每个阶段末设**门禁（Gate）**，未通过不得进入下一阶段。

### 3.2 各阶段活动与产出
| 阶段 | 主要活动 | 关键产出 | 当前状态 |
|---|---|---|---|
| P0 立项 | 可行性、目标、约束 | 立项说明（本文 §1） | ✅ 完成 |
| P1 需求 | 范围边界、API 契约、数据模型 | `API契约文档.md`、`数据模型与采集设计文档.md` | ✅ 完成 |
| P2 设计 | 三段式架构、14 域、存储 | 架构基线（§4） | ✅ 完成 |
| P3 实现 | 功能里程碑、TDD/回归 | M9–M19 提交 | 🟡 进行中（M19 待纳入） |
| P4 集成 | 小程序 API 对齐、端到端 | 集成测试报告 | 🔲 待办（小程序对齐） |
| P5 上线 | 部署、CI、监控、备份 | 上线清单 + 运维手册 | 🔲 待办 |
| P6 运营 | 数据回流、积累域沉淀、复盘 | 运营月报 | 🔲 待办 |

### 3.3 质量门禁（Gate 的强制条件）
每次提交（智能体改动）必须满足：
1. **自动化**：全套回归测试全绿（pre-commit 钩子自动执行 `npm test`）。
2. **零依赖**：未引入任何 npm 包（`package.json` 无新依赖）。
3. **知识外置**：规则/数据不硬编码，来源于 `data/*.json` 或 14 域库，专家可审。
4. **人工审核**：智能体改动由业主/Capri 审阅；业主改动由 AI 协理审核（见 §7 协作纪律）。
5. **文档同步**：接口/数据/行为变更须同步对应文档。

> 任一条件不满足，提交**不得合入 main**。

### 3.4 人类在环（Human-in-the-Loop）
- **知识质检流水线**：每条 14 域规则带 `source`（来源）+ `confidence`（置信度，刻度：合规0.95>平台运营0.90>花艺师手册0.85>行业资料0.80>平台种子0.75>竞品0.70>潮流0.60）。
- `npm run review` 离线导出专家审阅清单（`data/review/`），领域专家在 `reviews.json` 复核后回写合并。
- 目的：确保"非专业人员设定的规则"经由专业复核达到科学可信。

### 3.5 变更控制
- 重大变更（范围增减、架构调整、引入依赖申请）须提交"变更请求"，由 Capri 审批后更新本计划书版本号（v1.1…）。
- 紧急修复可先提交后补文档，但须在 `开发日志.md` 记录。

---

## 4. 技术方案与架构

### 4.1 总体架构（智能体核心 + 小程序壳）
```
[微信小程序] ──HTTP/JSON──▶ [server.js: API + preview]
                                │
                ┌───────────────┼───────────────────┐
                ▼               ▼                   ▼
         [lib/agent]      [数据层 dataLayer]    [SQLite 主库]
         decomposer       14 域读取             (kv + feedback)
         planner          (库优先/JSON回退)
         chat.js(LLM润色)
         feedbackStore
                │
        [LLM/图 适配器] ──超时降级──▶ 规则引擎 / SVG
```
- **数据主权归服务端**：方案/订单/反馈最终落服务端库；小程序仅回传输入与反馈，可替换、可重做。

### 4.2 核心处理流水线（三段式：规则优先 + LLM 补全 + 澄清兜底）
1. **decomposer**：需求分解（场合/对象/风格/花材/数量 `quantity_spec`/预算 `detectBudget`/禁忌）。
2. **planner**：`composePlan` / `composeFromSpec` 现算（知识库单价×数量+包装），不依赖硬预设 bundle。
3. **chat.js**：LLM 仅做自然语言润色，事实由规则引擎注入，约束"不得编造"；失败回落规则引擎。

> 规则盲区不会"崩溃"（有 LLM 补全 + 澄清兜底），但可能"静默半错"——这是已知架构风险（见 §8 R1），由 M19 极端用例 + 后续盲区检测缓解。

### 4.3 14 域数据底座
| 类别 | 域 |
|---|---|
| 种子主数据（上线前建） | 花卉详情、门店价格、地区差异、商家能力、知识教育、合规信任、物流基础 |
| 积累数据（上线后沉淀） | 库存(H)、UGC(I)、潮流、营销权益(L)、竞品基准(M)、反馈(F)、元学习(N) |

- 存储：`lib/db.js`（node:sqlite，零依赖，WAL 一写多读）；`kv` 表存 plans/sessions/orders（JSON 列），`feedback` 结构化表支持 SQL 聚合。
- 迁移：首次启动自动从旧 JSON 迁移并改名 `.migrated` 备份。

### 4.4 存储与部署约束
- **零依赖**：禁引入 npm 包；仅用 Node 内置 `node:sqlite` 等。
- **运行产物隔离**：`.env`（密钥）、`data/agent.db`（主库）、`data/review/*`（生成物）均被 `.gitignore` 忽略，绝不入仓。

### 4.5 API 契约（集成协议）
- 14 个接口，已实现的：chat / plan / shops / orders / pay / health / preview / feedback / feedback.stats。
- 作为智能体与**任何**客户端（小程序/Web/第三方）的对接协议，契约变更须同步文档并过门禁。
- 待补：鉴权平台化（`code2session` 换 token）尚未实现。

### 4.6 学习闭环
- `/feedback` 原子写落库 + 聚合 + `getSignals`；`/chat` 回传 `feedback_signals` 与 `domain_insights`（潮流/地区/知识）。
- LLM 定位为无状态被调用工具；记忆/学习/经验库/聚合回写全在服务端，换 LLM 不影响自有数据资产。

---

## 5. 里程碑与进度计划

### 5.1 已完成里程碑
| 标签 | 内容 | 状态 |
|---|---|---|
| `v0-current-clean` (21cff35) | 基线：架构边界确立、Web 移除 | ✅ |
| `m9` ~ `m13` | 插件门面 / 洞察插件 / 数据扩展 / 插件外观 / 质量门禁雏形 | ✅ |
| `m10-api-hardening` | API 限流中间件（固定窗口） | ✅ |
| `m14-quality-gate` | pre-commit 门禁（自动跑 `npm test`） | ✅ |
| `m-chat-context` (fc39fc9) | 多轮双轨对话（业主并行开发，单独提交） | ✅ |
| `m15-knowledge-qc` (62fa42f) | 知识质检流水线 | ✅ |
| `m16-miniprogram-audit` | 小程序 UI 审计 + 优化清单 | ✅ |
| `m17-quantity-planning` | 数量解析 + 知识库现算方案 | ✅ |
| `m18-budget-nl` (85eabe3) | 预算改为规则权威 + 自然语言更正必触发重算出图 | ✅ |
| `c13b137`（用户工作区） | 业主并行开发当前状态（未打 mN 标签，独立回退） | ✅ 已推送 |

### 5.2 待办 / 进行中里程碑
| 里程碑 | 内容 | 状态 | 备注 |
|---|---|---|---|
| M19 自然语言强化 | 极端自然语言用例（业主已写 `test/extreme-rule.js`） | 🟡 | 待 AI 协理纳入里程碑并打标签 |
| M20 鉴权平台化 | `code2session` 换 token、接口鉴权 | 🔲 | API 契约前置骨架已就绪 |
| M21 小程序对齐 | 小程序 API 对齐 + 补学习闭环 | 🔲 | 接 `feedback`、修正 `plan.steps` 渲染 |
| M22 上线部署 | CI/CD、监控、备份、preview 部署 | 🔲 | 当前仅本地运行 |
| M23 运营回流 | 积累域数据沉淀、运营复盘 | 🔲 | 上线后启动 |

---

## 6. 质量管理计划

### 6.1 测试策略
- **单元测试 / 回归**：`npm test` 串联全部套件（agent / plugins / http / ratelimit / chat / review / quantity / extreme-rule 等），当前全绿。
- **极端用例**：M19 极端自然语言（中文数词、否定、过敏、预算歧义）专项覆盖。
- **测试即门禁**：pre-commit 钩子强制全绿方可提交。

### 6.2 质量门禁（详见 §3.3）
自动化（测试）+ 零依赖 + 知识外置 + 人工审核 + 文档同步。

### 6.3 知识质检流水线
`source`/`confidence` 元数据 + `npm run review` 导出 + 专家 `reviews.json` 复核回写。

### 6.4 零依赖底线
任何"想引 npm 包"的需求，先评估 Node 内置替代；确需外部库时走变更控制审批。

---

## 7. 配置与版本管理

### 7.1 分支与提交纪律
- 主分支 `main`；功能在本地完成并过门禁后合入。
- **双轨并行纪律**：业主（Capri）与 AI 协理的改动**分开提交、分别打标签**，互不混入同一里程碑，保证回退精确。
- 提交前 `git status` 先排查对方未提交的活儿，避免覆盖/冲突。

### 7.2 里程碑标签与回退
- 每个验证过的智能体改动由 AI 协理自动 `git commit` 并打 `mN` 标签；业主改动由业主自提交（AI 可审核）。
- 回退依据 = `开发日志.md` + git 标签（`git checkout mN`）。

### 7.3 忽略项（安全）
`.gitignore` 已忽略：`.env`（密钥）、`data/agent.db`（主库）、`node_modules/`、`*.log`、`data/previews/`、`data/review/*`、`.workbuddy/`。
> 推送前已验证：`.env` 与 `data/agent.db` 未被纳入（2026-08-07 推送核查通过）。

---

## 8. 风险管理

| ID | 风险 | 概率 | 影响 | 缓解措施 |
|---|---|---|---|---|
| R1 | 规则盲区"静默半错" | 中 | 高 | M19 极端用例；后续盲区检测 + LLM 主理解演进（架构答疑方向 B/C） |
| R2 | 零依赖约束限制功能 | 低 | 中 | 优先 Node 内置；确需外部库走变更审批 |
| R3 | 双轨并行 git 冲突 | 中 | 中 | 分开提交 + 分别标签 + 提交前排查 |
| R4 | 密钥/数据库泄露 | 低 | 高 | `.gitignore` 忽略 + 推送前核查（已执行） |
| R5 | 外部 LLM/图 API 不可用 | 中 | 中 | 适配器超时（LLM20s/图30s）+ 降级规则引擎/SVG |
| R6 | 知识库科学性不足 | 中 | 中 | 知识质检流水线 + 专家复核（confidence 刻度） |
| R7 | 小程序真机联调网络失败 | 中 | 低 | apiBase 填电脑局域网 IP + 勾"不校验合法域名" |
| R8 | SQLite 并发/损坏 | 低 | 中 | WAL 模式；上线备份策略（M22） |
| R9 | 缺 CI/监控导致上线隐患 | 中 | 中 | M22 建 CI 跑测试 + 健康检查 + 日志 |
| R10 | 范围蔓延（UI 归属模糊） | 低 | 低 | 边界约定已确立（UI 业主自研，后端 AI 负责） |

---

## 9. 沟通与协作计划
- **异步协作**：业主并行开发，AI 协理按自身节奏推进里程碑；业主完成的内容交 AI 审核（跑回归、审逻辑、确认提交）。
- **文档同步**：接口/数据/行为变更即时更新 `API契约文档.md` / `数据模型与采集设计文档.md` / `开发日志.md`。
- **周度复盘**（建议）：P6 阶段起，每迭代末回顾风险表与里程碑进度。

---

## 10. 交付物清单
| 类别 | 交付物 |
|---|---|
| 文档 | 本计划书、API契约文档、数据模型与采集设计文档、知识质检流水线、小程序UI优化清单与评估报告、开发日志 |
| 代码 | `server.js` + `lib/`（agent/data/http/llm/plugins）+ `scripts/agent-cli.js` |
| 客户端 | `miniprogram/`（微信原生源码） |
| 数据 | 14 域种子 + SQLite 主库（运行期，不入仓） |
| 测试 | 全套回归套件（全绿）+ 极端自然语言用例 |
| 仓库 | GitHub `flora_diy_agent`（含 mN 标签与基线） |

---

## 11. 验收标准
项目（阶段）视为通过验收，须同时满足：
1. 全套回归测试常绿，且覆盖 M17–M19 自然语言关键路径。
2. API 契约 14 接口可用，文档与实现一致。
3. 14 域种子数据已入库，知识质检清单经专家复核。
4. 小程序可真机联调，接 `feedback` 学习闭环。
5. 任意 `mN` 标签可独立 `git checkout` 回退，无副作用。
6. 密钥/主库确认未入仓（`.gitignore` 生效）。

---

## 12. 上线与运营（后续阶段 P4–P6 要点）
- **M20 鉴权**：实现 `code2session` 换 token，接口加鉴权中间件。
- **M21 小程序对齐**：修正 `plan.steps` 渲染、接入 `/feedback`、对齐运行时字段。
- **M22 上线**：CI（推送即跑测试）、进程守护、SQLite 备份、preview 部署、基础监控/告警。
- **M23 运营**：积累域（H/I/L/M/N）数据沉淀，月度运营复盘，回流优化规则。

---

## 13. 附录：仓库与文档索引
- 仓库：`https://github.com/Capri770101/flora_diy_agent`（main @ `c13b137`）
- 关键文档：`API契约文档.md`、`数据模型与采集设计文档.md`、`知识质检流水线.md`、`小程序UI优化清单与评估报告.md`、`开发日志.md`
- 质量门禁脚本：`package.json` → `npm test`（pre-commit 自动触发）
- 质检脚本：`npm run review`

---
*本计划书为 v1.0 标准流程基线；后续重大变更按 §3.5 变更控制更新版本。*
