# 智能花卉 DIY 智能体 · 数据模型与采集设计文档

> 版本：v0.1（2026-08-06）
> 定位：14 域数据底座的**唯一结构契约**。与服务端代码、`API契约文档.md` 一一对应；是智能体作为"领域专家"可被任何客户端消费的数据地基。
> 存储：Node 内置 `node:sqlite`（零依赖、零编译、WAL 并发安全）。结构见 `lib/db.js`；种子加载见 `lib/seed.js`；统一读取见 `lib/dataLayer.js`。

---

## 1. 两类数据：种子主数据 vs 积累数据

| 类别 | 含义 | 建表时机 | 数据来源 |
|---|---|---|---|
| **种子主数据** | 上线前即可用行业资料建立的"静态底盘" | 建库即灌 | 行业资料 / 平台配置 / 迁移自 `data/*.json` |
| **积累数据** | 上线后由真实用户行为与交易沉淀 | 先建空表，运行时写入 | 用户反馈 / 订单 / UGC / 竞品爬取 / 元学习回写 |

> 数据主权归**服务端智能体**。微信小程序仅回传输入与反馈，可替换、可重写，平台数据不丢。

---

## 2. 14 域总览与落地映射

| # | 域 | 落地表 | 来源标记 | 建表时机 | 当前状态 |
|---|---|---|---|---|---|
| A | 花卉详情 | `flowers` | 种子 | 建库灌 | ✅ 已灌 24 条（迁移自 flowers.json） |
| B | 门店价格 | `shops` + `shop_stock` | 种子 | 建库灌 | ✅ 已灌 6 店（迁移自 shops.json） |
| C | 商家服务质量与风格 | `merchant_profiles` | 种子(派生) | 建库灌 | ✅ 已灌 6 条（由门店区派生风格） |
| D | 地区差异 | `regions` | 种子 | 建库灌 | ✅ 已灌 6 区（深圳） |
| E | 花卉季节 | `flowers.months_json` + `regions.season_modifier_json` | 种子 | 建库灌 | ✅ 并入 A/D |
| F | 市场潮流 | `trends` + 反馈聚合 | 种子基线+积累 | 建库灌基线 | ✅ 基线 8 条；运行累加 |
| G | 物流履约 | `logistics` | 种子 | 建库灌 | ✅ 已灌 6 区基础 |
| H | 供应链库存 | `supply_inventory` | 积累 | 建库灌基线+运行扣减 | ✅ 已落库（下单按花材×门店扣减） |
| I | 内容 UGC 库 | `ugc` | 积累 | 运行写入 | ✅ 已落库（订单+正向反馈沉淀晒单） |
| J | 知识教育 | `knowledge` | 种子 | 建库灌 | ✅ 已灌 7 条 |
| K | 合规信任 | `compliance` | 种子 | 建库灌 | ✅ 已灌 5 条 |
| L | 营销权益 | `marketing` | 积累 | 建库灌基线 | ✅ 已灌 3 条（运营活动累加） |
| M | 竞品基准 | `competitors` | 积累 | 建库灌基线 | ✅ 已灌 3 条（巡检累加） |
| N | 智能体元学习 | `meta_learning` + `feedback` 聚合 | 积累 | 运行回写 | ✅ 已回写（`recordFeedback` 每次聚合并写 `meta_learning`） |

---

## 3. 种子主数据域 · 字段设计

### 3.1 A 花卉详情 `flowers`
| 字段 | 类型 | 说明 / 枚举 |
|---|---|---|
| `id` | TEXT(PK) | 花材 ID，如 `rose`（方案 `items[].flower_id` 必须来自此表，防幻觉硬约束） |
| `name` / `en` | TEXT | 中文名 / 英文名 |
| `role` | TEXT | 主花 / 配花 / 叶材 |
| `season` | TEXT | 全年 / 春夏季 / 夏秋季 / 冬春季 / 秋冬季 |
| `price` | REAL | 全局库价（元） |
| `unit` | TEXT | 支 / 扎 / 枝 / 片 / 束 |
| `meaning` | TEXT | 花语 |
| `bloom_period` | TEXT | 花期，如 `5-7天` |
| `allergen` | INTEGER(0/1) | 是否易过敏/高花粉（探病场景自动规避） |
| `care` | TEXT | 养护要点 |
| `months_json` | TEXT(JSON) | 可售月份数组 `1..12`，空数组=全年（E 域） |
| `style_tags_json` | TEXT(JSON) | 风格标签：`温柔/浪漫/热烈/高级/清新/极简/田园/复古/可爱/热带` |
| `occasions_json` | TEXT(JSON) | 适用场景：`生日/母亲节/婚礼/探病/乔迁/纪念日/表白/家居装饰` |
| `colors_json` | TEXT(JSON) | `[{name,hex}]` 颜色 |

### 3.2 B 门店价格 `shops` + `shop_stock`
`shops`：
| 字段 | 类型 | 说明 |
|---|---|---|
| `shop_id`(PK) / `name` / `city` / `district` / `address` | TEXT | 门店档案 |
| `lat` / `lng` | REAL | 经纬度（选店距离计算） |
| `rating` | REAL | 评分 1–5 |
| `open_hours` / `status` | TEXT | 营业时间；`open/closed`（缺货/闭店不剔除，仅标记） |
| `support_flowers_json` | TEXT(JSON) | 门店可供货花材 ID 列表（缺货项标记"可替换"） |
| `price_map_json` | TEXT(JSON) | 门店销售价 `{flower_id: price}`（**服务端权威价**，客户端价被忽略） |
| `cost_map_json` | TEXT(JSON) | 供货价（供货价×毛利=建议售价） |
| `margin_rate` | REAL | 毛利率 |
| `pack_cost` | REAL | 包装费 |
| `style_tags_json` / `service_tags_json` | TEXT(JSON) | C 域派生：商家风格 / 服务标签 |

`shop_stock`（库存快照，积累域 H 的实时视图来源）：
| 字段 | 类型 | 说明 |
|---|---|---|
| `shop_id`+`flower_id`(PK) | TEXT | 组合键 |
| `price` | REAL | 该店该花当前售价快照 |
| `in_stock` | INTEGER | 是否有货 |

### 3.3 C 商家服务质量与风格 `merchant_profiles`
| 字段 | 类型 | 说明 |
|---|---|---|
| `shop_id`(PK) | TEXT | 关联 `shops` |
| `style_tags_json` | TEXT(JSON) | 擅长风格（由门店所在区 `regions.popular_styles` 派生 + 平台校准） |
| `service_tags_json` | TEXT(JSON) | 服务能力：`同城配送/精美包装/附养护卡/定制贺卡…` |
| `quality_score` | REAL | 服务质量分（初始=门店 rating，积累后按履约/反馈加权） |
| `delivery_supported` | INTEGER | 是否支持同城配送 |

### 3.4 D 地区差异 `regions`
| 字段 | 类型 | 说明 |
|---|---|---|
| `region_id`(PK) / `city` / `district` | TEXT | 区域 |
| `season_modifier_json` | TEXT(JSON) | 季节修正 `{winter_factor, note}`（深圳亚热带，冬季花价平稳） |
| `price_index` | REAL | 区域价格系数（0.9–1.15，影响预算换算与门店排序） |
| `popular_styles_json` | TEXT(JSON) | 区域偏好风格（派生商家风格、潮流基线） |
| `note` | TEXT | 区域客群备注 |

### 3.5 F 市场潮流基线 `trends`
| 字段 | 类型 | 说明 |
|---|---|---|
| `trend_id`(PK) | TEXT | 如 `t_m5_carnation` |
| `region_ref` | TEXT | 关联 `regions`（基线 `sz_all` 表示全市） |
| `month` | INTEGER | 1–12 |
| `flower_id` | TEXT | 关联 `flowers` |
| `score` | REAL | 热度 0–1（基线由行业经验设定，上线后由真实下单/反馈累加） |
| `source` | TEXT | `seed` / `behavior` |

### 3.6 G 物流履约基础 `logistics`
| 字段 | 类型 | 说明 |
|---|---|---|
| `zone_id`(PK) | TEXT | 如 `z_futian` |
| `region_ref` | TEXT | 关联 `regions` |
| `carrier` | TEXT | 同城闪送 / 同城达达 |
| `lead_time_days` | INTEGER | 时效（0.5=半日达，1=次日达） |
| `fee` | REAL | 配送费 |
| `supports_fresh` | INTEGER | 是否支持生鲜鲜花（默认 1） |

### 3.7 J 知识教育 `knowledge`
| 字段 | 类型 | 说明 |
|---|---|---|
| `knowledge_id`(PK) | TEXT |  |
| `category` | TEXT | 养护 / 技法 / 场景 / 履约 |
| `title` / `body` | TEXT | 知识条目 |
| `tags_json` | TEXT(JSON) | 标签 |
| `source` | TEXT | 行业资料 / 花艺师手册 / 平台规则 |

### 3.8 K 合规信任 `compliance`
| 字段 | 类型 | 说明 |
|---|---|---|
| `rule_id`(PK) | TEXT |  |
| `category` | TEXT | 消费者权益 / 价格合规 / 履约承诺 / 退换货 / 隐私 |
| `title` / `detail` | TEXT | 合规要点 |
| `applies_to` | TEXT | 适用场景 |
| `source` | TEXT | 平台规则 / 价格法 / 广告法 / 消保条例 / 个人信息保护法 |

---

## 4. 积累数据域 · 空表契约（上线后写入）

| 表 | 关键字段 | 写入触发 |
|---|---|---|
| `supply_inventory`(H) | `flower_id, shop_id, stock_qty, updated_at` | 门店日终盘点 / 接单扣减 |
| `ugc`(I) | `author, type, ref_id, content, rating, created_at` | 用户晒单 / 评价 |
| `marketing`(L) | `title, benefit_type, benefit_detail, start_at, end_at, region_ref` | 运营配置活动 |
| `competitors`(M) | `name, region_ref, price_index, rating, note` | 竞品价格巡检 |
| `meta_learning`(N) | `domain, key, metric, value, updated_at` | `feedback` 聚合回写（`getSignals` 信号落库，供方案阶段降权/提权） |

> F 域（潮流）与 N 域（元学习）的真实信号已闭环：每次 `/feedback` 落库后，`feedbackStore.recordFeedback` 立即聚合并回写 `meta_learning`（采纳率/均分/各花材 penalty）；`planner` 在选花时消费 `getSignals().low_adoption_flowers` 自动降权排除历史负面花材；`/chat` 同时回传 `domain_insights`（潮流/地区/知识）让方案更"懂行"。

---

## 5. 数据访问约定（供 lib/agent 消费）

- **唯一入口**：`lib/dataLayer.js`。优先读 SQLite；表为空/未建时回退 `data/*.json`，保证行为一致、可灰度。
- **读取即重建原形状**：`flowersAll()` 返回与 `data/flowers.json` 完全相同的对象数组（含 `花语/花期/过敏/months/styleTags/occasions/colors`），`shopsAll()` / `templatesAll()` 同理 → 现有 164 项回归测试零改动通过。
- **服务端权威价**：门店价一律以 `shops.price_map` 为准，客户端传入价被忽略（见 `API契约文档.md` §2 价格安全）。
- **重灌/更新**：`node scripts/seed-db.js`（幂等，`INSERT OR REPLACE/IGNORE`，可重复执行）。

---

## 6. 待办（数据底座后续）

- [x] 14 域表结构建库（`lib/db.js`）
- [x] 种子数据灌库（`lib/seed.js` + `scripts/seed-db.js`）：花卉/门店/地区/知识/合规/物流/潮流基线
- [x] 智能体改读 SQLite（`lib/dataLayer.js`，JSON 回退，164 回归绿）
- [x] 积累域接入写入（H 下单扣减 `supply_inventory`；I 订单+正向反馈写 `ugc`；L/M 已灌基线 3 条；N 每次反馈回写 `meta_learning`）— 2026-08-06 完成
- [x] F/N 信号回灌：`planner` 选花时降权排除 `low_adoption_flowers`（penalty≥0.5 且样本≥3）— 2026-08-06 完成
- [x] 新增域接入 CLI 与 `/chat`：`domain_insights`（潮流/地区/知识）返回字段 + 回执💡贴士；CLI `/feedback` 命令 — 2026-08-06 完成
- [ ] 真实 LLM/文生图接入下的端到端体验打磨（当前默认规则引擎）
- [ ] 鉴权平台化（API 调用鉴权）
- [ ] 竞品/营销域的实时采集管线（当前为种子基线）
