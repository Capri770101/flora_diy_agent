# 知识质检流水线

> 解决核心担忧：**非花卉专业人员，如何确保平台规则/数据"科学、有据、可复核"？**
> 答案：给每一条规则/数据打上 **`source`（来源）** 与 **`confidence`（置信度）** 两个元数据，再离线导出一份专家审阅清单，逐条复核、回写结论。规则"从哪来、我们多确定"一目了然，且可被任何人复核与修订。

---

## 1. 为什么需要它

智能体的推荐质量依赖 14 域规则库（花卉/门店/地区/潮流/知识/合规/物流/营销/竞品……）。
这些规则由平台方（非花卉专业）编写，来源混杂：有的来自法规、有的来自花艺师手册、有的只是平台估算。
如果不标注来源与把握程度，一旦出错无法追溯，也无法让专家高效介入。

质检流水线的设计原则（与本项目的"质量门禁"一致）：
- **有据可查**：每条都有 `source`。
- **有把握度量**：每条都有 `confidence`（0–1）。
- **有人复核**：导出清单 → 专家/运营复核 → 回写结论。
- **离线、不改库表**：质检是"离线审阅流程"，不写入运行时 SQLite，不影响线上服务。

---

## 2. 置信度刻度

| 等级 | 值 | 含义 | 典型来源 |
| --- | --- | --- | --- |
| 法规级 | 0.95 | 直接来自法规/平台强制规则，几乎确定 | 合规 K（价格法/广告法/消保条例/个保法） |
| 平台运营级 | 0.90 | 平台自行定义、可完全负责 | 营销 L、平台规则类知识 |
| 专业知识级 | 0.85 | 花艺师手册等可核验专业经验 | 技法/养护类知识 |
| 行业资料级 | 0.80 | 通用行业知识，普遍成立但有例外 | 养护/季节类知识 |
| 平台种子级 | 0.75 | 平台基于常识给出的基准值（含定价/估算） | 花卉 A、地区 D、物流 G |
| 竞品估算级 | 0.70 | 对第三方竞品的估计，可能失真 | 竞品 M |
| 潮流基线级 | 0.60 | 启发式基线，最不确定，上线后由真实行为校准 | 潮流 F |

> 低于 **0.60** 的条目会在清单里用 ⚠️ 标记，建议**优先复核**。

---

## 3. 字段说明

| 字段 | 位置 | 说明 |
| --- | --- | --- |
| `source` | `lib/seed.js` 各域数组 + `data/flowers.json` 每条 | 来源标签，如 `行业资料` / `花艺师手册` / `平台规则` / `价格法` / `平台种子` / `平台估算` / `seed` |
| `confidence` | 同上 | 0–1 的把握程度，按 §2 刻度赋值 |

> 这两个字段仅用于离线审阅，**不写入运行时库表**（见 `lib/db.js` 14 域表结构，无 `source`/`confidence` 列）。运行时方案生成完全不依赖它们——这样质检流程可以自由迭代，不影响线上。

---

## 4. 覆盖范围（当前）

`npm run review` 聚合以下 8 个域、共 **123 条**：

| 域 | 条数 | 内容 |
| --- | --- | --- |
| A 花卉详情 | 52 | `data/flowers.json` 全部花材（含季节/价格/过敏/花语） |
| D 地区差异 | 7 | 深圳各区价格系数、偏好风格 |
| F 市场潮流 | 31 | 12 个月流行花材基线 |
| G 物流履约 | 6 | 各区配送商、时效、运费 |
| J 知识教育 | 16 | 养护/技法/场景/合规知识 |
| K 合规信任 | 5 | 过敏提示、价格透明、时效、退换、隐私 |
| L 营销权益 | 3 | 新人/母亲节/周末活动 |
| M 竞品基准 | 3 | 竞品价格/评分估算 |

> 随数据底座扩样，条数会自动增长，清单无需改代码。

---

## 5. 使用流程（四步闭环）

### 第 1 步：生成清单
```bash
npm run review
```
生成三个文件（均在 `data/review/`，已被 `.gitignore` 忽略，属生成物）：
- `review_checklist.md` —— 按域分组的审阅表（含摘要/上下文、复核状态、⚠️ 优先项）
- `review_checklist.csv` —— 扁平表，便于在 Excel/表格里筛选排序
- `reviews.example.json` —— 复核回写文件的**模板**（保留在版本库）

### 第 2 步：专家/运营复核
把 `reviews.example.json` 复制为 `reviews.json`，按 `<域>::<id>` 填复核结论：
```json
{
  "A花卉::rose":        { "status": "confirmed", "reviewer": "花艺师-王", "note": "无误" },
  "F潮流::t_m12_camellia": { "status": "revised", "reviewer": "运营-李", "note": "12月流行度偏低，score 建议下调至 0.65" },
  "M竞品::c_orose":     { "status": "pending", "reviewer": "", "note": "" }
}
```
`status` 取值：
- `pending` —— 待复核（默认）
- `confirmed` —— 确认无误
- `revised` —— 已修订，需同步回 `data/flowers.json` 或 `lib/seed.js`
- `rejected` —— 删除或下线

### 第 3 步：合并回写
```bash
npm run review   # 再次运行，会把 reviews.json 的结论合并进清单
```
清单里对应行的"状态/复核人/复核备注"会更新，总览也会刷新已复核比例。

### 第 4 步：据复核修订原始数据
- `revised`/`rejected` 项 → 回到 `data/flowers.json` 或 `lib/seed.js` 修正（如调整价格、流行度 score、下线下架）。
- 修订后重新 seed 主库（`node scripts/seed-db.js`），并跑回归（`npm test`）。

---

## 6. 命令速查

| 命令 | 作用 |
| --- | --- |
| `npm run review` | 导出/刷新专家审阅清单 |
| `node scripts/seed-db.js` | 把种子（含 source/confidence）灌入运行库 |
| `npm test` | 回归测试（含 `test/review.test.js`，校验聚合与合并逻辑） |

---

## 7. 与质量门禁、运行时的关系

- **离线、零风险**：导出脚本只读取种子数组与 JSON，不写库；`require` 种子时顺带打开的临时库已被隔离到 `os.tmpdir()/flora_qc_export`，不碰生产 `data/agent.db`。
- **可测**：`test/review.test.js` 断言覆盖 8 域 123 条、字段齐全、置信度刻度、合并逻辑，纳入 `npm test`。
- **可回退**：本流水线不改动运行表结构，纯增量；如需撤销，按《开发日志.md》的 `m15` 标签回退即可。

---

## 8. 注意与后续

- `data/flowers.json` 中的 `price` 为平台参考价，受渠道波动影响，复核时建议以门店 `price_map` 为准。
- 潮流 F 域为启发式基线（0.60），**上线后应靠真实订单/浏览行为**持续校准（对应 F 积累域与 N 元学习回写）。
- 未来若要让置信度参与运行时决策（如"低置信度花材在方案中降权"），可再加列落库——属独立演进，不在本流水线范围内。
