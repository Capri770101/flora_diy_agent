# 项目文件清理说明

> 清理日期：2026-08-06
> 执行人：智能体（Capri 授权）
> 目的：去除冗余 / 死代码 / 运行产物，确保项目目录清爽、可纳入版本控制，符合线下落地项目的规范性要求。

## 一、已删除（冗余 / 死代码 / 运行产物）

| 文件 / 目录 | 类型 | 删除原因 | 是否可重生 | 是否入版本控制 |
|---|---|---|---|---|
| `web/` | 死代码 | 网页前端已于 2026-08-05 下线，server.js 不再提供静态路由，无任何代码引用 | 否（设计上已废弃） | 清理前也未纳入 gitignore，属应删 |
| `FILE_WALKTHROUGH.md` | 过时文档 | 早期“逐文件讲解清单”，引用的 `web/` 与旧 `data/plans.json` 均已不存在，内容失准 | 否（被 README + 源文件取代） | 原已跟踪，已 `git rm` |
| `server.err.log` / `server.out.log` | 运行日志 | 服务运行产物，`*.log` 已被 `.gitignore` 忽略，可自动重生 | 是（重启服务即生成） | 否（已忽略） |
| `data/previews/*` | 生成效果图 | 每次出图自动生成，`data/previews/` 已被 `.gitignore` 忽略 | 是（按需重生） | 否（已忽略） |
| `data/*.json.migrated` | 迁移备份 | JSON→SQLite 迁移后的旧运行文件备份，SQLite 已稳定，备份无再用 | 否（历史备份） | 否（已忽略） |

> 说明：`web/` 与 `FILE_WALKTHROUGH.md` 的删除会从 git 索引移除；其余文件本就未被跟踪（gitignore），仅从磁盘移除。

## 二、保留的文件（有用、非冗余）

- `LEARNING_PLAN.md`：你的 Vibe Coding 个人学习路线，契合低代码运营定位，保留。已顺手修正其中对已删除 `web/`、`FILE_WALKTHROUGH.md` 的过时引用。
- `GITHUB_GUIDE.md`：git / GitHub 多设备操作手册，对低代码操作者有用，保留。
- 根目录 `project.config.json` / `project.private.config.json`：小程序遗留配置（appid 等）。
  **说明**：标准导入方式是从 `miniprogram/` 文件夹打开（其下有独立 `project.config.json`）。若你一直是“从根目录导入小程序”，则保留根配置、可删 `miniprogram/` 下的重复配置；反之保留 `miniprogram/` 下的。二选一，勿两套并存导致混淆。
- `data/agent.db*`：智能体运行期数据库（你的核心资产：记忆 + 学习信号），保留且已 gitignore。
- `data/flowers.json` / `shops.json` / `templates.json`：种子知识源文件（重灌数据库用），保留。
- 全部 `lib/`、`server.js`、`scripts/`、`test/`、`miniprogram/`、`*.md` 文档、`package.json`、`LICENSE`：项目主体，保留。

## 三、清理后的残留说明（无害）

- 根目录 `server.err.log` / `server.out.log` 两个运行日志在本次清理中因操作系统级“安全删除”保护未能移除；它们已被 `.gitignore` 忽略、不会进入版本控制，且重启服务会自动重生。如需彻底清空，可在文件资源管理器中手动删除，或重启服务后由新日志覆盖。

## 四、清理后验证（确认没改坏）

- 回归测试：`node test/agent.test.js` → 预期 164 passed（数据与逻辑未动）。
- 服务冒烟：起 `node server.js` 后 `node test/smoke2.js` → 全链路通过。
- 小程序联调：开发者工具导入 `miniprogram/`，`apiBase` 指向 `http://localhost:3000`，勾选“不校验合法域名”即可。

## 五、后续规范建议

1. **零依赖底线**：任何新功能不得引入 npm 依赖（`node:sqlite` 内置），外部能力走插件式 `lib/plugins/`。
2. **改动即验证**：每次改动后跑回归测试，红了不算完成。
3. **改动即提交**：每个验证过的里程碑由智能体 `git commit` 并打 `mN` 标签，便于精确回退（见《开发日志.md》）。
4. **知识外置**：花材 / 门店等专业知识放在 `data/*.json`，由领域专家审阅，不写死在代码里。
