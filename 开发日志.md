# 智能花卉 DIY 智能体 · 开发日志

> 用途：记录项目从 0 到当前的全部开发里程碑，作为“可回退”的依据。
> 你怎么用：当你想回到某个进度时，直接告诉我里程碑编号或大致日期即可，例如“回退到 M5（SQLite 之前）”或“回到 8 月 5 日那版”。我会依据本日志恢复对应状态。
> 配合 git：日志里的里程碑与 git 提交 / 标签对应；当前已打基线标签 `v0-current-clean`，代表本次清理后的干净状态。

## 回退机制说明（重要）

- **当前及以后**：每个验证过的改动，智能体会自动 `git commit` 并打标签（格式 `mN-简述`）。要精确回退，执行 `git checkout mN` 即可。
- **历史里程碑（M0–M7）**：因早期提交较稀疏，未逐个打标签，本日志以“逻辑里程碑”形式记录（含关键文件与回退要点）。如需精确回到某历史里程碑，告诉我编号，我会按日志重建对应文件状态（部分旧文件可从 git 历史 `git log` / `git show` 取回）。
- 所有运行数据（`data/agent.db`）与密钥（`.env`）均不进版本控制，回退代码不影响你的数据资产。

## 里程碑总览

| 编号 | 日期 | 标题 | 关键产出 |
|---|---|---|---|
| M0 | 08-05 | 工程文档 | 智能花卉DIY智能体_工程文档.md（15 章） |
| M1 | 08-05 | 系统初实现 | server.js + lib/ + web/ + miniprogram/ + 种子 JSON |
| M2 | 08-05 | 安全修复 | 路径穿越 / 价格伪造 / 图片超时修复 |
| M3 | 08-05 | 解耦与契约 | 移除 web/、CLI 主入口、API契约文档.md |
| M4 | 08-05 | 反馈闭环 | /feedback + feedbackStore（JSON 版） |
| M5 | 08-05 | SQLite 升级 | lib/db.js 取代 JSON 运行存储 |
| M6 | 08-06 | 14 域数据底座 | lib/seed.js + dataLayer + 种子灌库 |
| M7 | 08-06 | 进化能力 ①②③④ | 积累域写入 / 信号回灌 / CLI 反馈 / 懂行洞察 |
| M8 | 08-06 | 清理与日志 | 删冗余、建开发日志、基线标签 `v0-current-clean` |
| M9 | 08-06 | 真 LLM+文生图 | 接通 deepseek/dashscope + fetch 超时加固 + 启动日志显能力，标签 `m9-llm-image` |
| M10 | 08-06 | API 限流加固 | 限流中间件 + 全套测试纳入(agent/plugins/http/ratelimit)，标签 `m10-api-hardening` |

## 里程碑详情

### M0 · 工程文档（08-05）
- 产出：智能花卉DIY智能体_工程文档.md（需求 / 架构 / 接口 / 安全 / 部署 15 章）
- 回退：该文档仍保留，恢复此状态即保留文档即可。

### M1 · 系统初实现（08-05）
- 改动：搭建 Node 零依赖后端；web 网页前端；微信小程序骨架；花材 / 门店 / 模板种子 JSON；规则引擎 + 可插拔 LLM。
- 关键文件：server.js、lib/*、web/、miniprogram/、data/{flowers,shops,templates}.json
- 回退：此状态含 web/（已于 M3 / M8 移除）。

### M2 · 安全修复（08-05）
- 改动：修复 路径遍历（预览图路径）、价格伪造（服务端按门店价重算、忽略客户端价）、图片生成超时（加超时 + 降级 SVG）。
- 关键文件：server.js、lib/imageGen.js、lib/planner.js、lib/pricer.js
- 回退：回到 M1 后重做本步修复。

### M3 · 解耦与 API 契约（08-05）
- 改动：移除 web 前端（本次 M8 从磁盘删除）；CLI（scripts/agent-cli.js）成为主要开发 / 调试入口；产出 API契约文档.md 作为与任何客户端唯一对接协议。
- 关键文件：scripts/agent-cli.js、API契约文档.md、server.js（去静态路由）
- 回退：恢复 web/ 并撤销契约文档。

### M4 · 反馈闭环（08-05）
- 改动：新增 POST /api/v1/feedback 与 feedbackStore；反馈聚合为采纳率 / 均分 / 花材级负面信号，供学习回灌。
- 关键文件：lib/agent/feedbackStore.js、server.js、API契约文档.md（补 /feedback）
- 回退：撤销 feedback 相关路由与存储。

### M5 · SQLite 升级（08-05）
- 改动：用 Node 内置 node:sqlite 建 lib/db.js，运行期 plans / sessions / orders / feedback 由 JSON 整文件改为 SQLite（WAL、原子写、可聚合）；首次启动自动迁移旧 JSON 并改名 .migrated。
- 关键文件：lib/db.js、lib/agent/feedbackStore.js、server.js
- 回退：回到 M4 的 JSON 存储实现（注意 .migrated 备份可还原旧 JSON）。

### M6 · 14 域数据底座（08-06）
- 改动：建 14 域结构化表；种子灌库（flowers24 / shops6 / regions6 / knowledge7 / compliance5 / logistics6 / trends8 / 库存61 / 营销3 / 竞品3）；统一数据访问层 dataLayer，4 个消费点切到 SQLite（JSON 回退）。
- 关键文件：lib/db.js（建表）、lib/seed.js、lib/dataLayer.js、scripts/seed-db.js、数据模型与采集设计文档.md
- 回退：`node scripts/seed-db.js` 重灌即可重建种子；撤销 dataLayer 切换则回退到 JSON 读取。

### M7 · 进化能力 ①②③④（08-06）
- ① 积累域写入 + N 元学习回写：下单扣库存、写 UGC、feedback 聚合回写 meta_learning。
- ② 信号回灌：planner 选花时降权历史负面花材（low_adoption_flowers）。
- ③ CLI 接 /feedback：agent-cli.js 新增反馈命令，沉淀学习信号。
- ④ 懂行洞察：/chat 返回 domain_insights（潮流 / 地区 / 知识），回执附 💡 贴士。
- 关键文件：lib/db.js（写入层）、lib/seed.js、server.js、lib/agent/feedbackStore.js、lib/planner.js、lib/agent/index.js、scripts/agent-cli.js
- 验证：回归 164 绿；冒烟全链路通过。

### M8 · 清理与日志（08-06，当前）
- 改动：删除冗余文件（web/、FILE_WALKTHROUGH.md、运行日志、previews、.migrated）；修正 LEARNING_PLAN 过时引用；新增《清理说明.md》《开发日志.md》；建立 git 基线提交与标签 `v0-current-clean`。
- 关键文件：新增 清理说明.md、开发日志.md；删除 web/、FILE_WALKTHROUGH.md 等。
- 回退：`git checkout v0-current-clean` 即回到本干净状态。

### M9 · 真 LLM + 文生图接通与加固（08-06，标签 `m9-llm-image`）
- 改动：`.env` 中 5 个 key 已齐备且有效（LLM=deepseek、图片=阿里通义万象 dashscope），实测真 LLM 出方案 1.2s、真文生图 9.7s 返回真实 URL。
- 健壮性加固：`lib/plugins/llm/openai-compatible.js` 给外部 fetch 加 **20s 超时**，`lib/plugins/image/dashscope.js` 加 **30s 超时**，超时即降级到规则引擎/SVG，不再无限挂起拖死出方案流程；错误信息不泄露 key。
- 可观测性：server.js 启动日志打印 `LLM 通道：已启用(deepseek-chat)` / `文生图：已启用(dashscope)`，一眼区分真模型与兜底。
- 新增探针脚本 test/probe_llm.js、test/probe_image.js（换 key/换环境可快速验证链路）。
- 关键文件：lib/plugins/llm/openai-compatible.js、lib/plugins/image/dashscope.js、server.js、test/probe_*.js
- 验证：回归 164 绿；起真实服务实测启动日志与真 LLM 链路正常。
- ⚠️ 安全：`.env` 已加入 `.gitignore`，**API key 未进版本库**；提交 `118ec95`。

### M10 · API 限流加固（08-06，标签 `m10-api-hardening`）
- 改动：新增 `lib/http/middleware.js` 固定窗口限流中间件；新增 `test/ratelimit.test.js`（5 用例）验证限流/独立 IP/关闭开关；`test/http.test.js` 修正路径参数 `:id` 断言；`package.json` 把 agent/plugins/http/ratelimit 全纳入 `npm test`；同步 API契约文档.md 与 README.md。
- 关键文件：lib/http/middleware.js、test/ratelimit.test.js、test/http.test.js、package.json
- 验证：全套测试通过（agent 164 / plugins 8 / http 20 / ratelimit 5）。
- 回退：`git checkout m10-api-hardening` 即回到本状态；`git checkout m9-llm-image` 回到限流前。
- 提交：`552cc65`。
